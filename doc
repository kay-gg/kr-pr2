krpr [args] [text] 
by default (no args) returns just korean text

strip args
change state of state machine based on args
do stuff to text to change it into how its supposed to be pronounced
output text to terminal